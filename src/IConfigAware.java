/**
 * Game-wide configuration.
 */
public interface IConfigAware {
  int SCALE = 100; // how big to draw the game - SCALE=1 means each cell is 1px by 1px.
  int GRID_GAP = 2; // size in px of the gap between grid cells
}
